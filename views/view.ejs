<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta property="og:type" content="video.other">
    <meta property="og:url" content="<%=site%>/<%= video.unique%>">
    <meta property="og:video" content="<%= video.url%>" />
    <meta property="og:video:url" content="<%= video.url%>" />
    <meta property="og:video:secure_url" content="<%= video.url%>" />
    <meta property="og:video:width" content="<%= video.width%>" id="metaWidth"/>
    <meta property="og:video:height" content="<%= video.height%>" id="metaHeight"/>
    <meta property="og:video:type" content="video/mp4" />
    <meta property="og:title" content="<%= video.name || video.fileName%>">
    <title><%=video.name || video.fileName%></title>
    <%- include('libs') %>
</head>
<body>
    <div class="container">
        <video width="100%" height="100%" controls>
            <source src="<%= video.url%>" type="video/mp4">
        </video>
        <h4 class="fw-light">Uploaded by: <%= user%> | Upload Date: <%=`${video.date.getMonth() + 1}/${video.date.getDate()}/${video.date.getFullYear()}`%></h4>
        <h1><%= video.name%></h1>
        <h3><%= video.fileName%></h3>
    </div>
    
</body>
</html>